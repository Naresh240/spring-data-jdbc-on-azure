apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-data-jdbc-on-azure.fullname" . }}-test-connection"
  labels:
{{ include "spring-data-jdbc-on-azure.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "spring-data-jdbc-on-azure.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
