apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-data-jdbc-on-azure.fullname" . }}
  labels:
    {{- include "spring-data-jdbc-on-azure.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-data-jdbc-on-azure.selectorLabels" . | nindent 4 }}
